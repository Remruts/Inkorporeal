SDL_TEXTUREACCESS_TARGET

SDL_SetTextureBlendMode(SDL_Texture*, blendmode); SDL_BLENDMODE_BLEND
SDL_SetTextureAlphaMod(SDL_Texture*, alpha);
SDL_SetTextureColorMod(SDL_Texture*, r, g, b);
SDL_SetRenderTarget(SDL_Renderer*, SDL_Texture*);
SDL_SetRenderTarget(SDL_Renderer*, NULL); //para volver
